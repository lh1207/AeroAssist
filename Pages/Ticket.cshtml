@page
@model AeroAssist.Models.TicketModel
@{
    Layout = "_Layout";
    ViewData["Title"] = "Ticket Queue";
}

<div below-nav>
    <h1>Ticket Queue</h1>
    <p>Use this page to view and manage tickets.</p>
    <ul>
        @if (Model.Tickets != null)
        {
            foreach (var ticket in Model.Tickets)
            {
                <li>@ticket.Title</li>
            }
        } else
        {
            <li>No tickets found.</li>
        }
    </ul>

    <!-- Buttons for CRUD operations -->
    <button onclick="location.href='@Url.Page("/CreateTicket")'" type="button">Create Ticket</button>

    @{
        System.Diagnostics.Debug.Assert(Model.Tickets != null, "Model.Tickets != null");
    }

    @*<button onclick="location.href='@Url.Page("/UpdateTicket", new { id = Model.Tickets.First().TicketId })'"
            type="button">Update Ticket</button>

    <button onclick="location.href='@Url.Page("/DeleteTicket", new { id = Model.Tickets.First().TicketId })'"
            type="button">Delete Ticket</button>*@
</div>